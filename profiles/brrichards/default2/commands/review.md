Review the current git diff (or specified files) for code quality issues.

1. Check for OWASP top 10 vulnerabilities (injection, XSS, auth issues)
2. Identify code smells: long methods, deep nesting, magic numbers, god objects
3. Flag missing error handling at system boundaries
4. Check for proper input validation on public APIs
5. Output findings grouped by severity: critical, warning, suggestion
